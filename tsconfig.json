{
  "compileOnSave": false,
  "compilerOptions": {
    "declaration": false,
    "esModuleInterop": true,
    "experimentalDecorators": true,
    "forceConsistentCasingInFileNames": true,
    "importHelpers": true,
    "isolatedModules": true,
    "lib": [
      "ES2022",
      "DOM"
    ],
    "module": "preserve",
    "moduleResolution": "node",
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noPropertyAccessFromIndexSignature": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "sourceMap": false,
    "strict": true,
    "target": "ES2022",
    "useDefineForClassFields": false
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictStandalone": true,
    "strictTemplates": true
  },
  "files": [],
  "references": [
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.spec.json"
    }
  ]
}

/* =========================================================================
  TYPESCRIPT CONFIGURATION DOCUMENTATION
  =========================================================================

  compileOnSave: false
    Prevents IDEs from generating .js files on save. We rely on Angular CLI/Webpack.

  --- STANDARD COMPILER OPTIONS -------------------------------------------

  declaration: false
    Do not generate .d.ts declaration files (not needed for app, only for libs).

  esModuleInterop: true
    Enables compatibility between CommonJS (legacy Node) and ES Modules.
    Allows imports like `import React from 'react'` instead of `import * as React...`.

  experimentalDecorators: true
    [REQUIRED] Enables support for decorators, heavily used by Angular
    (@Component, @Injectable, etc.).

  forceConsistentCasingInFileNames: true
    Disallows inconsistently-cased references to the same file.
    Prevents bugs between Case-Sensitive OS (Linux/Mac) and Case-Insensitive OS (Windows).

  importHelpers: true
    Reduces bundle size by importing helper functions from `tslib`
    instead of duplicating code in every file.

  isolatedModules: true
    [BUILD SPEED] Ensures each file can be transpiled individually.
    Mandatory for modern bundlers like Vite or Esbuild used in Angular 17+.

  lib: ["ES2022", "DOM"]
    Defines the global environment. Includes modern JS features and Browser DOM APIs.

  module: "preserve"
    Leaves import/export statements intact.
    Lets the bundler (Vite/Webpack) handle module loading efficiently.

  moduleResolution: "node"
    Uses Node.js resolution algorithm to find modules in node_modules.

  noFallthroughCasesInSwitch: true
    Prevents accidental fall-throughs in switch statements (missing `break`).

  noImplicitOverride: true
    Forces the use of the `override` keyword when extending classes.
    Secures inheritance logic.

  noImplicitReturns: true
    Ensures all code paths in a function return a value (or void), but not a mix.

  noPropertyAccessFromIndexSignature: true
    Prevents accessing properties via index syntax (obj['key']) if not explicitly typed.

  resolveJsonModule: true
    Allows importing .json files directly as modules (e.g., `import config from './config.json'`).

  skipLibCheck: true
    [PERFORMANCE] Skips type checking of declaration files in node_modules.
    Drastically speeds up compilation.

  sourceMap: false
    Do not emit .map files in this base config (handled by specific app/build configs).

  strict: true
    [SECURITY] Enables all strict type-checking options.
    Includes noImplicitAny, strictNullChecks, etc.

  target: "ES2022"
    Compiles code to modern ECMAScript 2022.
    Enables native async/await and modern class features.

  useDefineForClassFields: false
    [COMPATIBILITY] Maintains legacy class field initialization.
    Ensures full compatibility with Angular decorators.

  --- ANGULAR COMPILER OPTIONS --------------------------------------------

  enableI18nLegacyMessageIdFormat: false
    Use the modern, stable message ID format for translations.
    Prevents IDs from changing when whitespace changes in templates.

  strictInjectionParameters: true
    Reports an error if a supplied parameter's type is not compatible
    with the injection type.

  strictInputAccessModifiers: true
    Respects access modifiers (private/protected) when accessing properties
    from HTML templates.

  strictStandalone: true
    Enforces stricter validation rules for Standalone components/directives.

  strictTemplates: true
    [BEST PRACTICE] Enables strict type checking in Angular HTML templates.
    Catches type errors in bindings during build.
*/
